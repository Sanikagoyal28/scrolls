

function UpdCard (props) {
    return <>
        <div className="updCard">
        <p className="updHead">{props.head}</p>
        <p className="updText">{props.text}</p>
        </div>
    </>
}

export default UpdCard